<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12+ cloud-backup and device-transfer exclusions.

  Keyboard state — shared preferences, learned word data, autocorrect history,
  frequency caches — must never leave the device. Even a restore to a second device
  by the same user could expose typed-history from the first device's context in the
  second device's learned model, which is a privacy defect not a convenience feature.

  Excluding ALL shared-prefs is intentional. User settings (haptics, theme, etc.) are
  acceptable to lose on reinstall; privacy is not an acceptable trade for convenience here.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
