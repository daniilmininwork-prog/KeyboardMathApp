<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12+ cloud-backup and device-transfer exclusions.

  The overlay app persists only the shared TallyPreferences file (overlay enabled flag,
  precision, percent mode). Excluding all shared-prefs keeps preferences device-local, matching
  the :app module's privacy posture so a restore can never carry settings between users/devices.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
